plugins {
    id 'java'
}

group 'beanvest.lib.apprunner'

dependencies {
    implementation libs.bundles.logging
    implementation project(':beanvest:lib:util')
}

test {
    useJUnitPlatform()
    testLogging {
        exceptionFormat = 'full'
    }
}